<p id="notice"><%= notice %></p>


<div class="container">
  <h3>Record Label - <%= @record_label.id %></h3>
  <hr>
  <p>
    <strong>Record Label Name:</strong>
    <%= @record_label.name %>
  </p>

  <%= link_to 'Edit', edit_record_label_path(@record_label), class: 'waves-effect waves-light btn teal lighten-2' %> |
  <%= link_to 'Back', record_labels_path, class: 'waves-effect waves-light btn green' %>
</div>
